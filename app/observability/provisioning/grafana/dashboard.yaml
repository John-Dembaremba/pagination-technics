apiVersion: 1

providers:
  - name: "app-dashboards"
    folder: "App Monitoring"
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards/
